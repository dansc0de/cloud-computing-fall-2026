[project]
name = "mock-llm"
version = "0.1.0"
description = "CS 1660 week 5: OpenAI-shaped stand-in for the vLLM sidecar"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
]
